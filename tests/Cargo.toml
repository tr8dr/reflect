[package]
name = "reflect_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
reflect = { path = "../reflect" }
reflect_macros = { path = "../reflect_macros" }

[dev-dependencies]
trybuild = "1.0"
expand = "0.1"
